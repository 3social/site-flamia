'use client';

// This is a placeholder sparkles component for decorative particles
// Full implementation would require tsparticles initialization on client
export function Sparkles({
  className = '',
}) {
  return (
    <div className={className}/>
  );
}
